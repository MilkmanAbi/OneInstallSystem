# >>> ois path (managed) >>>
# Added by OIS so installed tools in this directory are found.
case ":$PATH:" in
  *":/tmp/ois-stress.23089/ho me/.local/bin:"*) ;;
  *) PATH="/tmp/ois-stress.23089/ho me/.local/bin:$PATH" ;;
esac
export PATH
# <<< ois path (managed) <<<
# >>> ois path (managed) >>>
# Added by OIS so installed tools in this directory are found.
case ":$PATH:" in
  *":/tmp/ois-stress.13853/ho me/.local/bin:"*) ;;
  *) PATH="/tmp/ois-stress.13853/ho me/.local/bin:$PATH" ;;
esac
export PATH
# <<< ois path (managed) <<<
# >>> ois path (managed) >>>
# Added by OIS so installed tools in this directory are found.
case ":$PATH:" in
  *":/tmp/ois-stress.4610/ho me/.local/bin:"*) ;;
  *) PATH="/tmp/ois-stress.4610/ho me/.local/bin:$PATH" ;;
esac
export PATH
# <<< ois path (managed) <<<
# >>> ois path (managed) >>>
# Added by OIS so installed tools in this directory are found.
case ":$PATH:" in
  *":/tmp/ois-stress.31439/ho me/.local/bin:"*) ;;
  *) PATH="/tmp/ois-stress.31439/ho me/.local/bin:$PATH" ;;
esac
export PATH
# <<< ois path (managed) <<<
# >>> ois path (managed) >>>
# Added by OIS so installed tools in this directory are found.
case ":$PATH:" in
  *":/tmp/ois-stress.13171/ho me/.local/bin:"*) ;;
  *) PATH="/tmp/ois-stress.13171/ho me/.local/bin:$PATH" ;;
esac
export PATH
# <<< ois path (managed) <<<
