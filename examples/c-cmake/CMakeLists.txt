cmake_minimum_required(VERSION 3.10)
project(hello_cmake C)
add_executable(hello-cmake src/main.c)
if(NOT DEFINED GREETING)
  set(GREETING "default")
endif()
target_compile_definitions(hello-cmake PRIVATE GREETING="${GREETING}")
